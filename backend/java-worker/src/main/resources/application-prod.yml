spring:
  datasource:
    url: jdbc:postgresql://dpg-d4n4b6k9c44c738gfi50-a/autoreplyx_db
    username: autoreplyx_db_user
    password: HWtDjqIOHVPLuHr0PVTqFk2HDctmW43Q
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      idle-timeout: 300000
      connection-timeout: 20000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      timeout: 5000ms

# Disable Redis auto-configuration if not available
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

# Application Configuration
app:
  frontend-url: ${FRONTEND_URL:https://autoreplyx.onrender.com}

# Logging
logging:
  level:
    root: INFO
    com.autoreplyx: INFO
